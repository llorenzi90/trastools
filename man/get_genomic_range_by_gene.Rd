% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_to_extract_info_from_gtf.R
\name{get_genomic_range_by_gene}
\alias{get_genomic_range_by_gene}
\title{Get genomic range by gene}
\usage{
get_genomic_range_by_gene(gtf_df, by = "gene_id")
}
\arguments{
\item{gtf_df}{A data frame containing the GTF (Gene Transfer Format) data.
This should include columns such as \code{seqid}, \code{start}, \code{end}, \code{strand}. \cr
GTF files can be loaded
with function \code{rtracklayer::readGFF}}

\item{by}{A character string specifying the column name to group by.
Default is \code{"gene_id"}. This column should contain the gene identifiers.}
}
\value{
A data frame with the genomic ranges of each gene in BED format,
containing columns: \code{seqid}, \code{start}, \code{end}, \code{gene_id}, \code{strand}, \code{width}.
}
\description{
Retrieves the genomic coordinates that each gene
spans from the start of the first (most 5') exon to the end of the last
(most 3') exon. The function returns a data frame in BED format with
the genomic ranges for each gene.
}
\examples{
# Example GTF data frame
# Get genomic range by gene
result <- get_genomic_range_by_gene(gtf_df)
print(result)
}
